import 'package:flutter/material.dart';

class TextStyle1 extends StatelessWidget {
  const TextStyle1({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
